---
name: Bug report
about: Something misbehaves
labels: bug
---

**Untangler version:** (from `gnome-extensions info untangler@bluvulture`)
**GNOME Shell version:** (`gnome-shell --version`)
**Distribution and version:**
**Session type:** Wayland / X11 (`echo $XDG_SESSION_TYPE`)
**Monitors:** count and scaling (e.g. 2 × 100%, 1 × 200%)

**What happened:**

**What you expected:**

**Steps to reproduce:**
1.

**Journal output:**
```
journalctl /usr/bin/gnome-shell -b 0 --no-pager | grep -i untangler
```
