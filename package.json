{
  "name": "untangler",
  "private": true,
  "license": "GPL-2.0-or-later",
  "engines": {
    "node": ">=20"
  },
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "for f in 'untangler@bluvulture'/*.js; do node --check \"$f\" || exit 1; done"
  }
}
