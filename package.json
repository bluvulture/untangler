{
  "name": "untangler",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/",
    "check": "for f in 'untangler@nebojsa.ilic'/*.js; do node --check \"$f\" || exit 1; done"
  }
}
